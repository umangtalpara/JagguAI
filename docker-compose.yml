version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: saas-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:7.0-alpine
    container_name: saas-redis
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
